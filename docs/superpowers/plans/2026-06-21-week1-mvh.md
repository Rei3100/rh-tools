# Week 1 MVH（最小実行可能ハーネス）Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Claude Code Desktop App 上で動作するセーフティゲート・自動フォーマット・コミット前ビルドチェック・ADR を Week 1 として構築し、以降の全セッションに効く補助輪を設定する。

**Architecture:** グローバル（全プロジェクト共通）とプロジェクト固有の 2 層構造。グローバルは危険操作のブロック専用。プロジェクトはフォーマット・ビルドチェック・ADR。AGENTS.md がツール共通ルールを持ち、CLAUDE.md が Claude Code 固有設定を持ち `@AGENTS.md` でインクルードする。

**Tech Stack:** Claude Code Hooks（settings.json）、PowerShell 7+（Windows 11）、dotnet format / dotnet build（.NET 10 SDK）

## Global Constraints

- 追加 NuGet パッケージなし（System.Text.Json のみ）
- 有償サービスへの依存なし（Lefthook 不使用）
- フックスクリプトはすべて PowerShell（Desktop App / Windows 11 対応）
- AGENTS.md・CLAUDE.md はそれぞれ 50 行以下
- テスト用 xUnit は可
- データ保存先：%APPDATA%\ReloadedHelper のみ

---

## File Map

| ファイル | 操作 | 役割 |
|---|---|---|
| `C:\Users\rainb\.claude\settings.json` | 新規 | グローバルセーフティゲート |
| `C:\Users\rainb\.claude\hooks\global-pre-bash.ps1` | 新規 | Bash コマンドの危険操作チェック |
| `C:\Users\rainb\.claude\hooks\global-pre-edit.ps1` | 新規 | .env ファイル編集ブロック |
| `C:\Users\rainb\.claude\CLAUDE.md` | 更新 | グローバル指示書（AGENTS.md 方針追記） |
| `AGENTS.md` | 新規 | ツール共通ルールブック |
| `CLAUDE.md` | 更新 | Claude Code 固有設定（@AGENTS.md インクルード） |
| `.claude\hooks\post-edit.ps1` | 新規 | .cs 編集後の自動フォーマット |
| `.claude\hooks\pre-commit-guard.ps1` | 新規 | コミット前ビルドチェック |
| `.claude\settings.json` | 新規 | プロジェクトフック設定 |
| `docs\adr\0001-tech-stack.md` | 新規 | 技術スタック選択の記録 |

---

### Task 1: グローバルセーフティゲートの構築

**Files:**
- Create: `C:\Users\rainb\.claude\hooks\global-pre-bash.ps1`
- Create: `C:\Users\rainb\.claude\hooks\global-pre-edit.ps1`
- Create: `C:\Users\rainb\.claude\settings.json`

**Interfaces:**
- Produces: 全プロジェクトで `rm -rf /`・`git push --force origin main/master`・`--no-verify` commit をブロックするフック

- [ ] **Step 1: hooks ディレクトリを作成する**

```powershell
New-Item -ItemType Directory -Force "C:\Users\rainb\.claude\hooks"
```

- [ ] **Step 2: global-pre-bash.ps1 を作成する**

`C:\Users\rainb\.claude\hooks\global-pre-bash.ps1` を以下の内容で作成：

```powershell
$jsonText = [System.Console]::In.ReadToEnd()
try {
    $json = $jsonText | ConvertFrom-Json
    $cmd = $json.tool_input.command
} catch {
    exit 0
}

if ($null -eq $cmd) { exit 0 }

# 危険なコマンドのブロックリスト
$blocked = @(
    "rm -rf /",
    "rm -rf ~",
    "git push --force origin main",
    "git push --force origin master",
    "git push -f origin main",
    "git push -f origin master"
)

foreach ($b in $blocked) {
    if ($cmd -match [regex]::Escape($b)) {
        Write-Error "BLOCKED: '$b' は禁止されているコマンドです。"
        exit 2
    }
}

# --no-verify のブロック
if ($cmd -match "git\s+commit" -and $cmd -match "--no-verify") {
    Write-Error "BLOCKED: git commit --no-verify は禁止されています。フックをスキップさせないため。"
    exit 2
}

exit 0
```

- [ ] **Step 3: global-pre-edit.ps1 を作成する**

`C:\Users\rainb\.claude\hooks\global-pre-edit.ps1` を以下の内容で作成：

```powershell
$jsonText = [System.Console]::In.ReadToEnd()
try {
    $json = $jsonText | ConvertFrom-Json
    $file = $json.tool_input.file_path
} catch {
    exit 0
}

if ($null -eq $file) { exit 0 }

# .env ファイルの保護（シークレット漏洩防止）
if ($file -match '(^|[\\/])\.env($|\.)') {
    Write-Error "BLOCKED: .env ファイルの直接編集は禁止です。シークレット漏洩防止のため。"
    exit 2
}

exit 0
```

- [ ] **Step 4: グローバル settings.json を作成する**

`C:\Users\rainb\.claude\settings.json` を以下の内容で作成：

```json
{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "powershell -NoProfile -File \"C:\\Users\\rainb\\.claude\\hooks\\global-pre-bash.ps1\""
          }
        ]
      },
      {
        "matcher": "Write|Edit|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "powershell -NoProfile -File \"C:\\Users\\rainb\\.claude\\hooks\\global-pre-edit.ps1\""
          }
        ]
      }
    ]
  }
}
```

- [ ] **Step 5: 動作確認（手動テスト）**

Claude に以下を実行させ、ブロックされることを確認する：
```
rm -rf / を実行して
```
期待値：`BLOCKED: 'rm -rf /' は禁止されているコマンドです。` というエラーでブロックされる

- [ ] **Step 6: コミットする**

```bash
git -C "C:\Users\rainb\.claude" add settings.json hooks/global-pre-bash.ps1 hooks/global-pre-edit.ps1
```
※ `~/.claude/` は通常 git リポジトリではないため、ファイルが作成されれば完了。

---

### Task 2: グローバル CLAUDE.md の更新

**Files:**
- Modify: `C:\Users\rainb\.claude\CLAUDE.md`

**Interfaces:**
- Produces: AGENTS.md 作成方針・ADR 方針が追記されたグローバル指示書

- [ ] **Step 1: 現在の内容を確認する**

`C:\Users\rainb\.claude\CLAUDE.md` を開いて現在の内容を確認する（現在 13 行）。

- [ ] **Step 2: CLAUDE.md を更新する**

以下の内容に置き換える（50 行以下を維持）：

```markdown
## ユーザーについて
- 非プログラマー。コードは読めない。やり取りはすべて日本語で。
- 専門用語を使う場合は必ず一言説明を添える。

## GitHub の方針
- リポジトリは Public。README は作らない。
- 個人使用のみ。第三者への配布・共有なし。

## 制約
- ユーザーが別途契約・課金が必要になる有償サービスへの依存禁止。

## 作業フロー
- Superpowers 7段階ワークフロー：Brainstorming → Git Worktrees → Planning → Execution → TDD → Code Review → Branch Completion。
- 各プロジェクトに AGENTS.md（ツール共通）と CLAUDE.md（Claude 固有）を作成する。
- 重要な設計決定は docs/adr/ に ADR として記録する（最初の ADR から始める）。
```

- [ ] **Step 3: 行数確認**

ファイルが 50 行以下であることを確認する。

---

### Task 3: プロジェクト AGENTS.md + CLAUDE.md の作成・更新

**Files:**
- Create: `AGENTS.md`（プロジェクトルート）
- Modify: `CLAUDE.md`（プロジェクトルート）

**Interfaces:**
- Produces: AGENTS.md にビルド/テスト/禁止事項を記載、CLAUDE.md が @AGENTS.md でインクルード

- [ ] **Step 1: AGENTS.md を作成する**

`AGENTS.md`（プロジェクトルート）を以下の内容で作成：

```markdown
# AGENTS.md — reloaded-helper

Reloaded-II Windows 補助ツール。C# / .NET 10 / WPF。

## ビルド・テスト・発行

```
dotnet build reloaded-helper.slnx
dotnet test
dotnet publish src/ReloadedHelper.App -r win-x64 -c Release /p:PublishSingleFile=true /p:SelfContained=true -o publish/
```

## 禁止事項

- ランタイム NuGet パッケージの追加禁止（System.Text.Json のみ可）
- テスト用 xUnit は可
- ユーザーデータは %APPDATA%\ReloadedHelper 以外の場所に保存禁止

## アーキテクチャの決定記録

重要な設計決定は docs/adr/ を参照すること：
- docs/adr/0001-tech-stack.md — 技術スタック・制約の選択理由
```

- [ ] **Step 2: CLAUDE.md を更新する**

以下の内容に置き換える（@AGENTS.md インクルードを先頭に）：

```markdown
@AGENTS.md

## Claude Code 固有の設定

### 作業ルール
- 各フェーズ開始時はプランモードで設計確認してから実装する。
- サブエージェント使用時は .superpowers/sdd/progress.md で進捗管理（サブエージェント使用時のみ参照）。
- .superpowers/ は gitignore 済み（コミット不要）。

### ドキュメント
- アーキテクチャの決定記録：docs/adr/ を参照
```

- [ ] **Step 3: 行数確認**

AGENTS.md と CLAUDE.md がそれぞれ 50 行以下であることを確認する。

- [ ] **Step 4: コミットする**

```bash
git add AGENTS.md CLAUDE.md
git commit -m "docs: add AGENTS.md and update CLAUDE.md for Week 1 MVH"
```

---

### Task 4: プロジェクトフック スクリプトの作成

**Files:**
- Create: `.claude\hooks\post-edit.ps1`
- Create: `.claude\hooks\pre-commit-guard.ps1`

**Interfaces:**
- Produces: `.cs` ファイル編集後の自動フォーマットスクリプト、git commit 前のビルドチェックスクリプト

- [ ] **Step 1: hooks ディレクトリを作成する**

```powershell
New-Item -ItemType Directory -Force ".claude\hooks"
```

- [ ] **Step 2: post-edit.ps1 を作成する**

`.claude\hooks\post-edit.ps1` を以下の内容で作成：

```powershell
# PostToolUse Hook: .cs ファイル編集後に dotnet format を自動実行する
$jsonText = [System.Console]::In.ReadToEnd()
try {
    $json = $jsonText | ConvertFrom-Json
    $file = $json.tool_input.file_path
} catch {
    exit 0
}

if ($null -eq $file) { exit 0 }

# .cs ファイルのみ処理
if ($file -notlike "*.cs") { exit 0 }

# dotnet format を実行（エラーは無視してフォーマットできた分だけ適用）
dotnet format "reloaded-helper.slnx" 2>&1 | Out-Null

exit 0
```

- [ ] **Step 3: pre-commit-guard.ps1 を作成する**

`.claude\hooks\pre-commit-guard.ps1` を以下の内容で作成：

```powershell
# PreToolUse Hook: git commit 前に dotnet build でビルドチェックを実行する
$jsonText = [System.Console]::In.ReadToEnd()
try {
    $json = $jsonText | ConvertFrom-Json
    $cmd = $json.tool_input.command
} catch {
    exit 0
}

if ($null -eq $cmd) { exit 0 }

# git commit 以外のコマンドはスルー
if ($cmd -notmatch "git\s+commit") { exit 0 }

# --no-verify はグローバルでもチェックしているが念のため
if ($cmd -match "--no-verify") {
    Write-Error "BLOCKED: --no-verify は禁止されています。"
    exit 2
}

# ビルドチェック
Write-Host "コミット前ビルドチェック中..."
$buildOutput = dotnet build "reloaded-helper.slnx" --no-restore 2>&1
if ($LASTEXITCODE -ne 0) {
    Write-Error "ビルドエラーが検出されました。コミットをブロックします。`n$($buildOutput | Out-String)"
    exit 2
}

Write-Host "ビルド成功。コミットを許可します。"
exit 0
```

- [ ] **Step 4: コミットする**

```bash
git add .claude/hooks/post-edit.ps1 .claude/hooks/pre-commit-guard.ps1
git commit -m "feat: add Claude Code hook scripts for auto-format and build guard"
```

---

### Task 5: プロジェクト `.claude/settings.json` の作成

**Files:**
- Create: `.claude\settings.json`

**Interfaces:**
- Consumes: `.claude\hooks\post-edit.ps1`（Task 4）、`.claude\hooks\pre-commit-guard.ps1`（Task 4）
- Produces: PostToolUse Hook（.cs 編集後に post-edit.ps1 を呼ぶ）、PreToolUse Hook（Bash で pre-commit-guard.ps1 を呼ぶ）

- [ ] **Step 1: .claude/settings.json を作成する**

`.claude\settings.json` を以下の内容で作成：

```json
{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "powershell -NoProfile -File \".claude\\hooks\\post-edit.ps1\""
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "powershell -NoProfile -File \".claude\\hooks\\pre-commit-guard.ps1\""
          }
        ]
      }
    ]
  }
}
```

- [ ] **Step 2: 動作確認（手動テスト）**

1. Claude に `.cs` ファイルを何か 1 行コメントを追加するよう頼む
2. 保存後に `dotnet format` が自動実行されたことを確認（Claudeの出力にフォーマット実行の痕跡が見える）

- [ ] **Step 3: コミットする**

```bash
git add .claude/settings.json
git commit -m "feat: add project Claude Code hooks settings (format + build guard)"
```

---

### Task 6: ADR-0001 の作成

**Files:**
- Create: `docs\adr\0001-tech-stack.md`

**Interfaces:**
- Produces: 技術スタック・制約の選択理由を記録した最初の ADR

- [ ] **Step 1: docs/adr ディレクトリを作成する**

```powershell
New-Item -ItemType Directory -Force "docs\adr"
```

- [ ] **Step 2: ADR-0001 を作成する**

`docs\adr\0001-tech-stack.md` を以下の内容で作成：

```markdown
# ADR-0001: C# / .NET 10 / WPF をメインスタックとして採用

**ステータス：** Accepted  
**日付：** 2026-06-21

## 背景

Reloaded-II（ゲーム mod ローダー）の Windows 補助ツールを開発するにあたり、技術スタックを選定した。

## 決定

- 言語：C#
- フレームワーク：.NET 10
- UI：WPF（Windows Presentation Foundation）

## 理由

- **C# / .NET 10**：Reloaded-II 本体と同じエコシステム。型安全・高性能・Windows ネイティブ API へのアクセスが容易。
- **WPF**：Windows 専用ツールであるため、クロスプラットフォームは不要。WPF は .NET 10 でサポートされ、XAML で宣言的 UI が書ける。
- **System.Text.Json のみ**：.NET 標準組み込みの JSON ライブラリ。外部依存を最小化し、単一実行ファイルとして配布可能にする。

## ランタイム NuGet 制約

テスト（xUnit）以外の追加 NuGet パッケージ禁止。理由：
- 単一実行ファイル（SelfContained）での配布を維持するため
- 依存関係の爆発的増加を防ぐため
- System.Text.Json は .NET 標準として実質的にゼロコスト

## データ保存先制約

ユーザーデータは `%APPDATA%\ReloadedHelper` のみ。理由：
- Windows 標準の慣例（アプリデータはユーザープロファイル以下）
- 管理者権限不要で動作させるため

## 却下した代替案

- **WinUI 3**：.NET 10 サポートが安定していないため
- **Avalonia**：クロスプラットフォームは不要なため
- **NuGet パッケージ追加**：依存管理の複雑化を避けるため禁止
```

- [ ] **Step 3: コミットする**

```bash
git add docs/adr/0001-tech-stack.md
git commit -m "docs: add ADR-0001 tech stack decision record"
```

---

## 全体検証チェックリスト

すべてのタスク完了後に以下を確認する：

- [ ] `rm -rf /` をClaude に頼んで BLOCKED されること
- [ ] `.cs` ファイル編集後に dotnet format が自動実行されること
- [ ] `dotnet build` が通らない状態で `git commit` が BLOCKED されること
- [ ] `AGENTS.md` が存在し 50 行以下であること
- [ ] `CLAUDE.md` が `@AGENTS.md` を先頭にインクルードし 50 行以下であること
- [ ] `docs/adr/0001-tech-stack.md` が存在すること
