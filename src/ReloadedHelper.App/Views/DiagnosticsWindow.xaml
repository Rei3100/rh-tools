<Window x:Class="ReloadedHelper.App.Views.DiagnosticsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="診断" Width="640" Height="700"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource BgMainBrush}"
        FontFamily="{DynamicResource MainFont}">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="180"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="HeaderText" Grid.Row="0" FontSize="16" FontWeight="Bold"
                   Foreground="{DynamicResource TextPrimaryBrush}" Margin="0,0,0,12"/>

        <TextBlock x:Name="StatusText" Grid.Row="1" Text="解析中..."
                   Foreground="{DynamicResource TextLabelBrush}" VerticalAlignment="Top"/>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <ItemsControl x:Name="FindingsList">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Background="{DynamicResource BgCardBrush}" CornerRadius="6"
                                Padding="12,10" Margin="0,0,0,8">
                            <StackPanel>
                                <TextBlock Text="{Binding Title}" FontWeight="Bold"
                                           Foreground="{Binding TitleBrush}" TextWrapping="Wrap"/>
                                <TextBlock Text="{Binding Message}" Margin="0,4,0,0"
                                           Foreground="{DynamicResource TextBodyBrush}" TextWrapping="Wrap"/>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <!-- 自動配置履歴セクション ヘッダー -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,12,0,6">
            <TextBlock Text="自動配置履歴" FontSize="14" FontWeight="Bold"
                       Foreground="{DynamicResource TextPrimaryBrush}" VerticalAlignment="Center"/>
            <Button x:Name="RestoreButton" Content="元に戻す" Margin="12,0,0,0"
                    Padding="10,4" Click="RestoreButton_Click"/>
        </StackPanel>

        <!-- 自動配置履歴リスト -->
        <ScrollViewer Grid.Row="3" VerticalScrollBarVisibility="Auto">
            <ItemsControl x:Name="HistoryList">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Background="{DynamicResource BgCardBrush}" CornerRadius="4"
                                Padding="10,8" Margin="0,0,0,4">
                            <StackPanel>
                                <TextBlock Text="{Binding Label}" FontWeight="SemiBold"
                                           Foreground="{DynamicResource TextPrimaryBrush}"/>
                                <TextBlock Text="{Binding Detail}" Margin="0,2,0,0"
                                           Foreground="{DynamicResource TextBodyBrush}"
                                           TextWrapping="Wrap" FontSize="11"/>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</Window>
