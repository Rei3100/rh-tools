<UserControl x:Class="ReloadedHelper.App.Views.HelpView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{DynamicResource BgMainBrush}">
    <StackPanel Margin="32,24" HorizontalAlignment="Left">
        <TextBlock Text="Reloaded Helper"
                   FontSize="20" FontWeight="Bold"
                   Foreground="{DynamicResource TextPrimaryBrush}"
                   Margin="0,0,0,8"/>
        <TextBlock Text="バージョン 0.2.0"
                   FontSize="{DynamicResource FontSizeDetailBody}"
                   Foreground="{DynamicResource TextLabelBrush}"
                   Margin="0,0,0,16"/>
        <Border Height="1" Background="{DynamicResource BgSeparatorBrush}" Margin="0,0,0,16" Width="200" HorizontalAlignment="Left"/>
        <TextBlock FontSize="{DynamicResource FontSizeDetailBody}"
                   Foreground="{DynamicResource TextDetailBrush}">
            <Run Text="ソースコード: "/>
            <Hyperlink NavigateUri="https://github.com/Rei3100/reloaded-helper"
                       Foreground="{DynamicResource AccentBrush}" TextDecorations="None"
                       RequestNavigate="Hyperlink_RequestNavigate">
                <Run Text="github.com/Rei3100/reloaded-helper"/>
            </Hyperlink>
        </TextBlock>
        <TextBlock Text="MIT License" Margin="0,8,0,0"
                   FontSize="{DynamicResource FontSizeDetailBody}"
                   Foreground="{DynamicResource TextLabelBrush}"/>
    </StackPanel>
</UserControl>
